import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const Transactions = () => {
  return (
    <div className="p-6 max-w-md mx-auto mt-20">
      <Title level={2}>Transactions Page</Title>
    </div>
  );
};

export default Transactions;
